---
description: 디자인 요소 PNG 목업을 생성해 사용자 컨펌 받기 (art-director, rules/50)
argument-hint: "<요소, 예: 맵 레이아웃 / 적 4종>"
---
`art-director` 에이전트로 "$ARGUMENTS" 목업을 생성하고 컨펌받아라.
scripts/mockup.ps1로 PNG 생성(docs/design/) → 이미지로 사용자에게 제시 → 승인/수정 → CONFIRMATIONS.md 상태 갱신(⏳→✅). 모든 비주얼은 승인 전 구현 보류.
