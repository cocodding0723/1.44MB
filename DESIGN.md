# NEON DESCENT — 상세 게임 기획서 (GDD v1.0 — FROZEN)

> 장르: 네온 벡터 실시간 액션 로그라이크 · 트윈스틱 슈터
> 테마: **기계 심부로의 하강** — 손상된 거대 기계 속을 정화하며 끝없이 내려간다
> 콘테스트: 2pgarcade **1.44MB Game Contest 2026** (마감 2026-09-04)
> 스택: **no-CRT + Win32 + OpenGL 즉시모드**, MSVC / x64
> 상태: v1.0 (FROZEN) — 전체 리뷰 통과·동결. 사운드/미니맵/상점/옵션 스펙 명세 완료. 다음 단계: M1 엔진 골격 구현.

---

## 목차
1. [개요 & 비전](#1-개요--비전)
2. [테마 & 세계관](#2-테마--세계관)
3. [설계 원칙](#3-설계-원칙)
4. [게임플레이 개요 — 코어 루프](#4-게임플레이-개요)
5. [플레이어](#5-플레이어)
6. [무기 · 발사체 · 전투 수식](#6-무기--발사체--전투-수식)
7. [모듈(강화) 시스템](#7-모듈강화-시스템)
8. [적 — 4 아키타입 + 엘리트 어픽스](#8-적--4-아키타입--엘리트-어픽스)
9. [보스 — 2종 로테이션](#9-보스--2종-로테이션)
10. [던전 생성 알고리즘](#10-던전-생성-알고리즘)
11. [방 타입 · 전투방 로직 · 스폰](#11-방-타입--전투방-로직--스폰)
12. [난이도 커브 (깊이별 표)](#12-난이도-커브)
13. [점수 · 비트 · 진행](#13-점수--비트--진행)
14. [카메라 & 게임필(juice)](#14-카메라--게임필)
15. [아트 디렉션](#15-아트-디렉션)
16. [사운드 (절차 합성)](#16-사운드)
17. [게임 상태 & 화면](#17-게임-상태--화면)
18. [HUD & UX](#18-hud--ux)
19. [컨트롤 (최종)](#19-컨트롤)
20. [기술 아키텍처 (no-CRT)](#20-기술-아키텍처)
21. [튜닝 상수 종합표](#21-튜닝-상수-종합표)
22. [용량 예산 & 최적화](#22-용량-예산--최적화)
23. [마일스톤 & 수용 기준](#23-마일스톤--수용-기준)
24. [리스크 & 대응](#24-리스크--대응)
25. [리뷰 노트 & 동결](#25-리뷰-노트--동결)

---

## 1. 개요 & 비전
**한 줄:** 손상된 거대 기계의 심부로, 네온 빛 정화 프로그램이 되어 끝없이 내려가는 한 판 5~15분의 트윈스틱 액션 로그라이크.

**핵심 경험:** 빠른 이동·대시 회피와 정밀 조준 사격의 손맛 + 매 판 달라지는 절차생성 지형과 랜덤 모듈 빌드. 죽으면 처음부터(permadeath), "한 판만 더"의 중독성.

**왜 이 컨셉인가 (콘테스트 관점):**
- 네온 벡터 = 텍스처/에셋 0개 → 용량 자동 초소형(목표 <64KB)
- 절차생성 = 적은 코드로 무한 콘텐츠 → "완성도 vs 스코프" 균형
- 즉발적 액션 + 파티클 글로우 = 작아도 화려 → "재미" 심사 강세

---

## 2. 테마 & 세계관
**기계 심부로의 하강.** 거대한 기계(메인프레임/메가스트럭처)가 손상됐다. 당신은 **정화 스파크(purge spark)** — 시스템 최심부로 내려가 손상을 제거하는 프로그램. 하지만 손상은 무한히 재생되고, 기계는 끝없이 깊다. 그래서 엔드리스: **클리어는 없고, 더 깊이 내려간 기록만 남는다.**

### 플레이버 명명 (거의 0코드, 정체성 강화)
| 일반 용어 | 본 게임 표기 |
|---|---|
| 깊이/층 | **LAYER**(레이어) |
| 통화/골드 | **BITS**(비트) — 추출한 데이터 조각 *(1.44MB 콘테스트 테마와도 호응)* |
| 강화 | **MODULE**(모듈) 설치 |
| 적 | 손상 프로세스 / 오류 / 바이러스 |
| 벽 | 데이터 회로(circuit) |
| 보스 | 손상된 서브코어 |
| 계단 | 다운링크(downlink) 포트 |

### 레이어 = 시스템 구획 (팰릿 순환과 동조)
냉각층 → 연산층 → 메모리층 → 전력층 → 커널층 (5티어 순환, §15 팰릿).

### 적 아키타입 플레이버 (기능은 §8 동일)
- Chaser=**헌터(Hunter) 프로세스**, Shooter=**터릿(Turret) 데몬**, Bouncer=**리코셰(Ricochet) 패킷**, Splitter=**포크(Fork) 바이러스**.

> 서사는 얇게 유지: 타이틀/게임오버 1~2줄 + 비주얼·명명으로만 전달. 컷신·텍스트덤프 없음.

---

## 3. 설계 원칙
- **에셋 파일 0개.** 그래픽·사운드·맵·폰트 전부 코드 절차생성.
- **시스템형 > 핸드메이드.** 적은 4 아키타입 × 엘리트 어픽스로 변주, 모듈은 스택형으로 창발적 빌드, 보스 2종 로테이션. 손으로 짜는 콘텐츠 최소화.
- **MVP가 그 자체로 완결.** 모듈·보스·엘리트 없이도 "내려가며 싸우다 죽는" 완전한 루프 성립 → 완성도 점수 방어.
- **juice 우선.** 같은 메커닉이라도 셰이크·파티클·히트스톱으로 체감 2배.
- **결정론적 재현.** 모든 랜덤은 시드 기반(xorshift) → 디버깅·밸런싱·리플레이 용이.

---

## 4. 게임플레이 개요
### 코어 루프
```
[레이어 진입] → 방 탐험(자유 이동) → 전투방 진입 시 문 잠김
   → 손상 프로세스 웨이브 전멸 → 문 개방 + BITS 드롭
   → 모듈방에서 MODULE 3택1 → 다음 방
   → 다운링크 발견 → 다음 레이어 하강(난이도↑·팰릿 변화)
   → 3레이어마다 서브코어(보스 2종 교대) → 처치 시 레어 모듈 + 회복 + 다운링크
   → 피격 누적 사망 → 점수 정산(레이어×킬×BITS) → R로 즉시 재시작
```

### 세션 길이
- 한 판 목표 **5~15분**. 평균 플레이어는 LAYER 4~6에서 사망, 숙련자는 9+.
- 무한 하강. "클리어" 없음 — 레이어·점수 갱신이 목표.

### 첫 60초 (튜토리얼 없이 학습)
1. 시작방(안전) 스폰 → WASD 이동 자연 학습
2. 첫 복도 → 전투방, 문 닫힘 → 헌터 4마리 → 마우스 조준 사격(LMB)
3. 위험 순간 Space 대시 회피 학습
4. 문 개방 → 모듈방 첫 MODULE 3택1 → "빌드가 쌓인다" 인지
5. 다운링크 → LAYER 2, 팰릿 변화로 진척 체감

---

## 5. 플레이어
| 속성 | 값 | 비고 |
|---|---|---|
| 충돌 반경 | 14 px | 원형 |
| 최고 이동속도 | 220 px/s | |
| 가속 | 2000 px/s² | ~0.11s에 최고속, 스냅감 |
| 감속(마찰) | 2400 px/s² | 키 떼면 빠르게 정지 |
| 최대 체력 | 6 HP (하트 3개, 1하트=2HP) | |
| 피격 데미지 | 보통 1 HP(접촉·탄), 보스 패턴 2 HP | |
| 무적 프레임 | 피격 후 1.0s (깜빡임) | |
| 대시 속도 | 700 px/s, 0.14s 지속 (~98px) | |
| 대시 무적 | 대시 중 + 종료 후 0.05s | |
| 대시 쿨다운 | 0.8s (모듈로 감소) | |

**이동 모델:** 입력 방향 목표속도 → 가속/감속 보간, 대각 정규화.
**대시:** 이동 입력 방향(없으면 마우스 방향)으로 순간 가속 + i-frame + 잔상. 쿨다운은 발밑 링 표시.

---

## 6. 무기 · 발사체 · 전투 수식
**단일 무기 확정.** 무기 다종 대신 **모듈로 무기를 변형**해 빌드 다양성 확보(§7).

| 속성 | 기본값 |
|---|---|
| 발사 방식 | LMB 홀드 자동 연사 |
| 연사 간격 | 0.20s (5 shots/s) |
| 발사체 속도 | 700 px/s |
| 발사체 반경 | 5 px |
| 발사체 데미지 | 10 |
| 발사체 수명 | 1.2s (사거리 ~840px) |
| 관통 | 0 |
| 명중 산포 | 0° (멀티샷 시 부채꼴) |
| 넉백 | 40 px/s 임펄스 |

### 전투 수식
```
최종데미지 = 기본데미지 × (1 + 0.25 × 데미지스택) × 치명배율
치명배율   = (치명 발동 시) 2.0, else 1.0  ; 발동확률 = 0.15 × 치명스택 (cap 1.0)
연사간격   = 0.20 / (1 + 0.20 × 속사스택)
발사체수   = 1 + 멀티샷스택   (부채꼴 산포 = min(12°×멀티샷, 60°))
관통수     = 0 + 관통스택
이동속도   = 220 × (1 + 0.12 × 민첩스택)   (상한 1.8배 = 396)
```
DPS(기본) = 10 × 5 = **50**. 헌터(HP20) 처치 ~0.4s. 빌드 성장으로 난이도 추격(§12).

---

## 7. 모듈(강화) 시스템
### 획득 경로 (페이싱: 레이어당 ~1개)
- **모듈방**(레이어마다 1개 보장): **커먼 3택1**
- **서브코어 처치**(3레이어마다): **레어 3택1** + 즉시 2HP 회복
- **엘리트 처치**: 깊은 레이어에서 낮은 확률로 커먼 모듈 오브 드롭(§8.5)
- (스트레치) 상점: BITS로 구매

### 커먼 풀 (14종) — 스택 가능
| # | 이름 | 효과 | 스택 |
|---|---|---|---|
| 1 | 고출력 | 데미지 +25% | 가산 |
| 2 | 속사 | 연사속도 +20% | 가산 |
| 3 | 민첩 | 이동속도 +12% (상한 1.8×) | 가산 |
| 4 | 멀티샷 | 발사체 +1 (산포 +12°) | 가산, 상한 +6 |
| 5 | 관통 | 관통 +1 | 가산 |
| 6 | 유도 | 발사체 곡선 추적(선회 90°/s, 스택당 +60°/s) | 가산 |
| 7 | 흡혈 | 12킬마다 1HP (스택당 임계 −3, 최소 3) | 가산 |
| 8 | 폭발 | 명중 시 폭발(반경 60, 뎀 50%); 스택당 반경+25, 뎀+15% | 가산 |
| 9 | 반사 | 벽 튕김 +2회 | 가산 |
| 10 | 냉각 | 대시 쿨 −25%(곱연산, 하한 0.2s) | 곱연산 |
| 11 | 가시 | 접촉 적에 15뎀+넉백(스택당 +15) | 가산 |
| 12 | 치명 | 치명확률 +15%(cap 100%), 2× | 가산 |
| 13 | 신속탄 | 발사체 속도·수명 +30% | 가산 |
| 14 | 자력 | BITS/픽업 흡인 +120%, 소형 감속 오라 | 가산 |

### 레어 풀 (6종) — 서브코어 보상
| 이름 | 효과 |
|---|---|
| 분열탄 | 적 명중 시 미니 발사체 2개 분열(뎀 40%) |
| 시간왜곡 | 피격 시 0.3s 슬로모(0.3×) |
| 과부하 | 매 10번째 발사 = 대형탄(뎀 4×, 반경 2×) |
| 재생 | 25초마다 1HP |
| 수호막 | 15초마다 재충전 보호막(1회 피격 무효) |
| 추가심장 | 최대 HP +2, 즉시 전체 회복 |

### 빌드 아키타입 (가중 추첨이 지향하는 5방향)
설계 의도를 위해 카드 추첨은 **현재 빌드와 시너지 강조**(보유 모듈에 따라 관련 모듈 가중↑):
1. **샷건/난사:** 멀티샷+산포보정(치명·고출력)
2. **관통 레일:** 관통+신속탄+고출력(일직선 학살)
3. **폭발 포병:** 폭발+과부하+멀티샷(군집 처리)
4. **유도 추적:** 유도+분열탄+속사(조준 부담↓)
5. **근접 가시/대시:** 가시+냉각+민첩+수호막(몸으로 비비는 빌드)

### 스택 철학
- 대부분 무한 스택(빌드 폭발 허용). 상한: 멀티샷(+6)·치명(100%)·민첩(1.8×)·냉각(0.2s).

### 모듈 추첨 알고리즘 & 가중치 ⭐신규
3택1은 균등이 아니라 **빌드 시너지 가중**:
```
draw3():
  pool = (상한 미도달) 커먼 모듈 전체
  w(m) = base_w(m) × synergy_mult(m, 보유빌드)
  중복 없이 가중 추첨 3개 (제시 슬롯 간 중복 금지)

synergy_mult: 보유 스택이 트리거하는 관련 모듈 ×2.0 (아키타입 §7)
  멀티샷 보유 → 관통·폭발·치명 ×2
  유도   보유 → 분열탄·속사 ×2
  폭발   보유 → 멀티샷·과부하 ×2
  가시   보유 → 냉각·민첩 ×2
```
| 모듈 | base_w | 메모 |
|---|---|---|
| 고출력·속사·민첩 | 10 | 항상 안전한 기본 |
| 관통·치명·신속탄 | 8 | |
| 멀티샷·흡혈·냉각·가시·자력 | 7 | 코어/유틸 |
| 유도·폭발·반사 | 6 | 강력·희소 |

**밸런스 가드 (degenerate 방지 — M5 플레이테스트 검증):**
- 폭발+과부하+멀티샷 고스택 = 화면 과부하 → 폭발 데미지 증가율 +15%→+12% 하향 검토
- 유도 풀스택은 조준 무력화 → **선회율 상한 360°/s**
- 반사+관통 동시 = 탄 영구잔존 → **반사탄도 BULLET_LIFE 소모**(무한 튕김 금지)
- 흡혈+가시+수호막 = 무한 생존 빌드 → 흡혈 임계 최소 3 유지(과회복 차단)

---

## 8. 적 — 4 아키타입 + 엘리트 어픽스
공통: 원형 충돌, 피격 시 흰 플래시(0.06s), 사망 시 파티클 16+링 1+BITS 드롭(1~3). 스케일 §12.

### 8.1 Chaser / 헌터 — 삼각형
| HP | 속도 | 접촉뎀 | 반경 | 등장 |
|---|---|---|---|---|
| 20 | 130 | 1 | 13 | LAYER 1+ |
- **AI:** 플레이어로 직선 스티어링 + 동족 분리(반경 30). 가장 흔함.

### 8.2 Shooter / 터릿 — 사각형
| HP | 속도 | 접촉뎀 | 반경 | 등장 |
|---|---|---|---|---|
| 16 | 70 | 1 | 14 | LAYER 2+ |
- **AI:** 거리 300px 유지(가까우면 후퇴). 1.6s마다 발사(탄속 280, 뎀 1, 반경 6), 발사 전 0.3s 텔레그래프.

### 8.3 Bouncer / 리코셰 — 다이아몬드
| HP | 속도 | 접촉뎀 | 반경 | 등장 |
|---|---|---|---|---|
| 12 | 230 | 1 | 11 | LAYER 3+ |
- **AI:** 직선 고속+벽 반사. 1.5s마다 방향 랜덤 재설정(플레이어 30% 편향).

### 8.4 Splitter / 포크 — 육각형
| HP | 속도 | 접촉뎀 | 반경 | 등장 |
|---|---|---|---|---|
| 30 | 60 | 1 | 18 | LAYER 4+ |
- **AI:** 느린 추격. 사망 시 **Shard 2**(HP8, 속도160, 반경8, 미니 헌터 AI). 재분열 없음.

### 8.5 엘리트 어픽스 시스템 ⭐신규
일반 적이 스폰 시 낮은 확률로 **어픽스**를 받아 엘리트로 등장 → 4 아키타입을 적은 코드로 무한 변주.
- **엘리트 확률** = `min(0.25, 0.03 × (깊이−1))` (LAYER 1 0% → LAYER 9+ 25%)
- **어픽스 수:** LAYER 1~6 = 1개, LAYER 7+ = 최대 2개(중복 불가)
- **시각:** 굵은 밝은 외곽선 + 어픽스 색 + 소형 공전 마커, 약간 큼(×1.15)
- **보상:** BITS ×3, 깊은 레이어에서 8% 확률 커먼 모듈 오브 드롭
- **제외:** Splitter의 Shard·보스에는 미적용

| 어픽스 | 효과 |
|---|---|
| 보호막(Shielded) | 1.5s마다 재충전되는 1회 피격 무효 막 |
| 신속(Swift) | 이동·공격속도 +40% |
| 폭발사망(Volatile) | 사망 시 폭발(반경 90, 뎀 2) — 회피 강제 |
| 자가분열(Cloning) | 4s마다 약한 복제 1체 생성(필드 상한 내) |
| 자성(Magnetic) | 플레이어를 약하게 당기는 오라 |

### 적 탄
- 반경 6, 네온 점멸, 가산 글로우. 벽에 닿으면 소멸. 플레이어 무적 중엔 통과.

---

## 9. 보스 — 2종 로테이션
3레이어마다(LAYER 3·6·9·12…) 서브코어 보스방. **2종을 번갈아** 등장 + 깊이 티어 스케일.
- 보스 인덱스 `k = LAYER/3` (1,2,3…). **홀수 k → THE CORE, 짝수 k → THE WARDEN.**
- 공통 HP = `400 × (1 + 0.5×(티어−1))`, 티어 = k. 접촉 데미지 2.

### 9.1 THE CORE — 탄막형 (구체)
반경 50 맥동, 방 중앙 부유.
- **P1 (100~66%) 탄막 링:** 2.5s마다 16발 방사 링(회전 오프셋).
- **P2 (66~33%) 레이저+소환:** P1 + 텔레그래프 레이저 스윕(예고 0.8s→발사 0.4s, 뎀 2), 6s마다 헌터 2 소환.
- **P3 (<33%) 격노:** 링 1.5s 주기, 더블 레이저, 이동↑, 적색 점멸.

### 9.2 THE WARDEN — 돌진/공간압축형 (회전 프레임) ⭐신규
반경 60, 회전 사각 프레임 + 공전 세그먼트. CORE의 "탄막 암기"와 대비되는 **"공간을 빼앗기는 압박"** 감각.

| 파라미터 | 값 |
|---|---|
| 평소 이동 | 느린 부유 40px/s (돌진 시 예외) |
| 공전 세그먼트 | 2개(P1)~3개(P3), 반경 130 공전, 접촉뎀 1 |
| 세그먼트 회전 | 90°/s(P1) → 130°/s(P2) → 70°/s 3개(P3) |

- **P1 (100~66%) 돌진:**
  - 1.6s 주기: 플레이어 위치 조준 → **0.7s 텔레그래프**(조준선) → **돌진 900px/s** 가로지름 → 벽 도달 후 0.5s 정지
  - 돌진 경로 **잔류 트레일** 0.6s 지속(접촉뎀 1)
- **P2 (66~33%) 회전 빔 + 돌진:**
  - **레이저 암 2개**(180° 대칭) 방출, **45°/s 스윕**(빔 두께 14, 접촉뎀 2)
  - 돌진 주기 1.6s→1.2s
- **P3 (<33%) 압축 격노:**
  - 방 가장자리에서 **위험 벽 수축 8px/s**(안전 반경 점감, 최소 반경 정지) → 중앙 강제
  - 레이저 암 3개·70°/s, 돌진 0.9s 주기, 세그먼트 3개, 적색 점멸·상시 약한 셰이크
  - 처치 시 수축 벽 즉시 해제

### 9.3 보상 (공통)
처치 → 폭발 연출(대형 파티클 + 화이트 플래시 + 히트스톱 0.2s) → **레어 3택1** + 2HP 회복 + 다운링크 출현.

---

## 10. 던전 생성 알고리즘
### 논리 그리드
- 방 그리드 최대 **9×9 셀**, 각 셀 0/1개 방.
- 레이어당 방 수 **N = min(12, 4 + 깊이)**.

### 절차 (random-walk 배치)
```
1. 중앙 셀 시작방, frontier 추가
2. N개까지: 빈 이웃 가진 방 랜덤 선택 → 빈 이웃에 새 방 + 문 기록
3. 루프: 인접 배치 방 쌍 15% 확률 추가 문(막다른 길 완화)
4. 특수방: 시작 BFS 최장거리=다운링크방(보스층=보스방),
           시작 외 임의 leaf=모듈방, 나머지=전투방(시작방은 안전)
```

### 타일 전개
- 각 방 → 랜덤 **11×9 ~ 17×13 타일**(타일 48px). 연결 방향에 문 갭(폭 2), 복도(폭 2~3)로 연결.
- 타일: `0=벽, 1=바닥, 2=문`. 충돌=원-타일 분리.

### 시드
- 런 시작 시 마스터 시드(QPC 하위비트). 레이어 시드 = hash(마스터, 깊이) → 동일 시드 동일 던전.

---

## 11. 방 타입 · 전투방 로직 · 스폰
| 방 타입 | 내용 |
|---|---|
| 시작방 | 적 없음, 안전. 스폰 |
| 전투방 | 진입 시 문 잠금 → 웨이브 → 전멸 시 개방 + BITS |
| 모듈방 | 적 없음, 중앙 제단(MODULE 3택1) |
| 다운링크방 | 다운링크(하강 트리거). 보스층은 보스방 |
| 상점방(스트레치) | BITS 구매대 |

### 전투방 트리거
- 방 중심 진입 & 미클리어 → 출입문 닫힘(네온 배리어), 웨이브 스폰.
- 적 수 §12. 스폰: 가장자리, 플레이어와 최소 150px, 0.4s 텔레그래프(바닥 마커→등장).
- 전멸 → 문 개방(사운드), BITS 흩뿌림, 0.1s 정화 플래시.

### 보스방
- 진입 시 전투방처럼 문 잠금 → 보스 처치까지 폐쇄.
- 처치 시 §9.3 보상(레어 3택1 + 2HP) + 방 중앙 **다운링크 출현**.

### 적 구성
- 해금된 타입에서 가중 추첨(초반 헌터↑, 후반 균등). 엘리트 확률 §8.5.

---

## 12. 난이도 커브
```
HP배율    = 1 + 0.18 × (깊이 − 1)
속도배율  = min(1.6, 1 + 0.04 × (깊이 − 1))
전투방적수 = min(12, 4 + 깊이)
엘리트확률 = min(0.25, 0.03 × (깊이 − 1))
```
| LAYER | 해금 | 적수 | HP배율 | 속도 | 엘리트 | 특수 |
|---|---|---|---|---|---|---|
| 1 | 헌터 | 5 | 1.00 | 1.00 | 0% | 온보딩 |
| 2 | +터릿 | 6 | 1.18 | 1.04 | 3% | |
| 3 | +리코셰 | 7 | 1.36 | 1.08 | 6% | **CORE T1** |
| 4 | +포크 | 8 | 1.54 | 1.12 | 9% | |
| 5 | +LANCER | 9 | 1.72 | 1.16 | 12% | |
| 6 | +WEAVER | 10 | 1.90 | 1.20 | 15% | **WARDEN T2** |
| 7 | 전체 | 11 | 2.08 | 1.24 | 18% | 어픽스2 |
| 8 | 전체 | 12 | 2.26 | 1.28 | 21% | |
| 9 | 전체 | 12 | 2.44 | 1.32 | 24% | **NEXUS T3** |
| 10 | 전체 | 12 | 2.62 | 1.36 | 25% | |
| 11 | 전체 | 12 | 2.80 | 1.40 | 25% | |
| 12+ | 전체 | 12 | +0.18/L | →1.60 | 25% | **CORE T4… (CORE/WARDEN/NEXUS 순환, §26)** |

**밸런스 의도:** 빌드로 DPS ~레이어당 +20~30% 성장 가정. 적 HP +18%/L → 빌드 성장하면 추격, 정체되면 자연 사망(엔드리스).

---

## 13. 점수 · 비트 · 진행
```
점수 = 깊이×1000 + 킬수×25 + BITS×5 + 보스킬×500 (+ 시간보너스, 스트레치)
```
- **BITS:** 적 처치 1~3, 엘리트 ×3 드롭(자력 모듈로 흡인). MVP=점수 환산, 스트레치=상점 통화.
- **진행:** 영구사망. 사망 시 점수 분해 표시 → R 재시작.
- **최고기록(스트레치):** `%LOCALAPPDATA%\neondescent.dat` 단일 바이너리.
  - 예시(합성): `{ magic:0x4E44, version:1, best_score:42100, best_layer:7 }` (12바이트)

### 픽업 규칙
- **BITS 오브:** 적 사망 위치에 1~3 드롭. **자력 반경**(기본 70px, 자력 모듈로 ↑) 내 자동 흡인 → 접촉 시 획득(coin SFX). 방 클리어 시 잔여분 자동 수거.
- **모듈 오브(엘리트 드롭):** 접촉 시 **랜덤 커먼 모듈 1개 즉시 설치**(3택1 아님 — 빠른 보너스). 글로우+회전 비주얼.
- 픽업 수명: 방 안 영구 잔존. 방 이탈 시 소멸.

### 회복 경제 (의도적 희소)
- **바닥 체력 드롭 없음.** 회복은 한정: 흡혈 / 재생(레어) / 추가심장(레어) / 보스 +2HP / 상점 수리(스트레치)뿐.
- 의도: 회복을 빌드·보스 보상에 묶어 **긴장 유지**. 과도하게 빡세면 M5에서 "12레이어마다 소형 수리팩" 추가 검토.
- **자가 피해 없음:** 폭발·반사 등 플레이어 발생 효과는 적에게만 데미지(자해 X).

### 상점방 (스트레치)
- 출현: 비보스 레이어당 **35% 확률**(전투방 1개 대체). 보스 레이어엔 없음.
- 판매 3종(가격 레이어 비례 +10%/L): **모듈** 60 BITS · **수리** 1하트(2HP) 45 BITS · **리롤/랜덤모듈** 30 BITS.
- 구매: 아이템 위로 이동 + E. BITS=점수 자원이라 **점수 vs 강함 트레이드오프**(전략성).
- 평균 수급: 레이어당 ~25~40 BITS → 모듈 1개에 ~2레이어 저축.

---

## 14. 카메라 & 게임필
### 카메라
- 플레이어 추적 + 보간(lerp 0.12). 마우스 방향 look-ahead(최대 60px). 방>화면이면 스크롤.

### Juice 사양
| 요소 | 사양 |
|---|---|
| 화면 셰이크 | trauma 0~1, offset = 20px × trauma² × noise, 감쇠 1.5/s. 피격+0.5, 폭발+0.3, 보스피해+0.2 |
| 히트스톱 | 처치 0.03s, 피격 0.08s, 보스 사망 0.2s (sim 정지, 렌더 지속) |
| 파티클 | 피격 8 / 사망 16+링 / 대시 잔상 / 총구 플래시 / 폭발 24. 가산 |
| 플래시 | 플레이어 흰 플래시, 적 흰 플래시, 화면 적색 비네트 펄스(피격) |
| 넉백 | 발사체 40, 폭발 120 |

---

## 15. 아트 디렉션
### 비주얼 언어 (기계 테마)
- 전부 **빛나는 도형** on 짙은 배경, 가산 블렌딩 글로우.
- 플레이어=다이아/삼각(흰 코어+컬러 글로우), 적=타입별 폴리곤, **글리치 점멸**로 손상 표현.
- 벽=**데이터 회로 라인**(네온) + 어두운 채움, 바닥=미세 도트 그리드(기판 느낌).
- 엘리트=굵은 외곽선+어픽스 색+공전 마커.

### 글로우 (셰이더 없이, 즉시모드)
- 도형을 **코어(밝게·작게) + 글로우(반투명·확대 2~3겹)** 가산 중첩.
- 스트레치: FBO→다운샘플 블러 1패스→가산 = 진짜 블룸.

### 팰릿 (레이어 = 시스템 구획, RGB 0~255)
| 티어/구획 | 배경 | 액센트(아군/회로) | 적 |
|---|---|---|---|
| 냉각층(시안) | (8,10,16) | (0,230,255) | (255,60,120) |
| 연산층(마젠타) | (14,8,16) | (255,60,200) | (120,255,80) |
| 메모리층(앰버) | (16,12,6) | (255,170,40) | (80,160,255) |
| 전력층(그린) | (6,16,10) | (60,255,140) | (255,90,90) |
| 커널층(바이올렛) | (10,8,18) | (170,90,255) | (255,210,60) |
- 플레이어 코어 항상 흰(255,255,255)+티어 글로우. 깊이 %5로 순환.

### 폰트 (파일 없음)
- HUD/짧은 단어 = **3×5 비트맵 글리프**(A–Z,0–9,기호) 코드 인코딩 → 네온 쿼드. 큰 글자는 스케일업.

---

## 16. 사운드
### 16.1 믹서 / 출력 (no-CRT waveOut)
- 포맷: **44100Hz · 모노 · 16bit PCM**. waveOut + **버퍼 3개 × 735 샘플**(≈16.67ms, 프레임당 1버퍼) 링.
- 채움: 메인 루프 끝에서 `WHDR_DONE` 버퍼 재충전 후 `waveOutWrite`(폴링). 끊기면 전용 스레드로 격상.
- 보이스: `voices[16]` = {active, sfxId, t(샘플카운트), phase, rngState, vol}. 매 샘플 합산 → [-1,1] 클램프 → int16.
- 마스터 볼륨 + **M 음소거**. 동일 SFX 8ms 내 중복 발사 시 1개로 병합(스팸 방지).

### 16.2 신스 프리미티브
```
osc_sine(ph)   = sin(2π·ph)
osc_square(ph) = ph<0.5 ? +1 : -1
osc_saw(ph)    = 2·ph − 1
osc_tri(ph)    = 1 − 4·|ph−0.5|
noise()        = xorshift → [−1,1]            (보이스별 rngState)
env_ad(t,a,d)  = t<a ? t/a : exp(−(t−a)/d)     // 지수 감쇠
sweep(t,f0,f1,T) = f0 + (f1−f0)·min(1, t/T)    // 선형 주파수 스윕
ph += freq/44100 (wrap 1.0)
```

### 16.3 SFX 파라미터 표 (전부 확정 — 데이터 0바이트, 코드 상수)
| 이벤트 | osc | 주파수 | 진폭·env_ad(a,d) | 길이 | 비고 |
|---|---|---|---|---|---|
| 사격 shoot | square | 700→400 sweep | 0.18 · (.002,.05) | 0.06s | 피치 ±5% 지터 |
| 적 피격 hit | noise | — | 0.15 · (.001,.04) | 0.05s | +1ms 클릭 |
| 적 사망 death | square+noise | 300→80 sweep | 0.22 · (.002,.16) | 0.18s | noise 0.10 레이어 |
| 플레이어 피격 hurt | sine+noise | 120→60 sweep | 0.35 · (.001,.22) | 0.25s | noise 0.30, 적색 플래시 동기 |
| 픽업/모듈 pickup | sine ×3 | 440·554·659 아르페지오 | 0.20 · (.005,.07) | 3×0.05s | 상승 |
| 대시 dash | noise | 대역상승 whoosh | 0.15 · (0→1→0) | 0.12s | |
| BITS 획득 coin | sine | 880→1320 | 0.12 · (.001,.05) | 0.05s | 짧고 밝게 |
| 문 개/폐 door | sine | 80→200 / 200→80 | 0.18 · (.01,.25) | 0.30s | |
| 보스 페이즈 phase | saw ×3 | 110·111.1·164.8 디튠 | 0.20 · (.05,.5) | 0.6s | 불안 코드 |
| 보스 사망 bossdie | saw+noise | 200→40 sweep | 0.50 · (.005,.6) | 0.7s | 대형, 히트스톱 동기 |

### 16.4 BGM (스트레치)
- **120 BPM**, 레이어 시드 결정. 구성: 팰릿 티어별 루트음 위 **아르페지오 8분음 sine** + 다운비트 **베이스 saw** + 비트 1·3 **킥**(sine 120→40 드롭 0.1s).
- 8마디 루프, 레이어마다 루트·패턴 변주. P3 보스전 템포 +10%·디튠.
- 미구현 시 **정적 드론**(저주파 saw 2음) 폴백.

---

## 17. 게임 상태 & 화면
```
TITLE ─start→ PLAYING ─ESC→ PAUSE ─ESC→ PLAYING
  ▲             │ │                              
  │             │ └─전투방 진입→(문잠금, 상태유지)
  │             ├─모듈방/보스보상→ UPGRADE_SELECT ─pick→ PLAYING
  └─R───────── GAMEOVER ◄─HP0─ PLAYING
```
- **TITLE:** 큰 네온 "NEON DESCENT", 부제 "PURGE THE MACHINE", "CLICK / SPACE — DESCEND", 최고기록, 배경 파티클.
- **PAUSE(ESC):** 디밍 오버레이, "PAUSED · ESC resume · Q quit". sim 정지.
- **UPGRADE_SELECT:** sim 정지, 카드 3장(이름+설명) 부유, 클릭/1·2·3 선택.
- **GAMEOVER:** "SIGNAL LOST — LAYER n", 점수 분해(레이어/킬/BITS/보스). **R**=새 런 즉시 시작(PLAYING), **ESC**=타이틀로.

---

## 18. HUD & UX
- **좌상:** 하트(HP), 아래 **LAYER n**
- **우상:** SCORE, **BITS**
- **플레이어 발밑:** 대시 쿨다운 링
- **전투방:** 잠긴 문=네온 배리어, 잔존 적수 미니 표시(선택)
- **미니맵:** 우상 방 그리드(탐험/현재/특수 색 구분) — 상세 §18 하단(M6)
- **가독성:** 적 탄 고대비+글로우, 플레이어와 색 분리, 피격 적색 신호 명확.

### 화면 레이아웃 목업 (ASCII) ⭐신규
**타이틀:**
```
          N E O N   D E S C E N T
              > PURGE THE MACHINE <

           CLICK / SPACE — DESCEND

        BEST   LAYER 07   SCORE 042100
```
**플레이 중 HUD:**
```
 ♥♥♥            LAYER 4              SCORE 018540
 ▓▓▓░ dash                            BITS  132
 ──────────────────────────────────────────────
 │        o      *                             │
 │   o   (◆)→  * * *       ▢ 터릿               │
 │       \          o 헌터                      │
 ──────────────────────────────────────────────
```
**모듈 선택 (sim 정지):**
```
            ─ INSTALL MODULE ─
    [1]            [2]            [3]
 ┌────────┐   ┌────────┐   ┌────────┐
 │ 멀티샷  │   │  폭발   │   │  치명   │
 │ 발사 +1 │   │명중시폭발│   │15% 2배 │
 └────────┘   └────────┘   └────────┘
        클릭 또는 1 · 2 · 3
```
**게임오버:**
```
            S I G N A L   L O S T
                LAYER 6 REACHED

     LAYER ×6000    KILLS 142×25=3550
     BITS 88×5=440    BOSS ×1=500
            ──────────────────
                SCORE  10490
              ( BEST 042100 )

            R — DESCEND AGAIN
```

### 미니맵 (M6)
- 위치: 우상단, 셀당 **8×8px**, 논리 방 그리드(최대 9×9).
- 방 상태색: **현재**=흰 채움 / **탐험됨**=액센트 흐림 채움 / **인접 미탐험**=외곽선만 / 미발견=비표시.
- 특수 마커: 모듈방 `◇` · 다운링크 `▼` · 보스방 `✖`(붉게) · 상점 `$`.
- 갱신: 방 진입 시 탐험됨 + 인접 방 외곽선 노출. (스트레치: Tab 확대)

### 옵션 & 접근성 (PAUSE 메뉴)
- **화면 흔들림:** 0% / 50% / 100% (`SHAKE_MAX` 배율)
- **플래시·글리치 강도:** 0% / 50% / 100% — **광과민성 대응**(번쩍임·적색 펄스 다수)
- **마스터 볼륨:** 0~100% (+ M 즉시 음소거)
- **전체화면:** F11 토글
- 적·탄은 **도형 코드 + 고대비 색**이라 색각 이상에도 구분 가능(별도 모드 불필요)
- 컨트롤러: **미지원**(키보드+마우스 전용). 스트레치로 XInput 트윈스틱 고려.

---

## 19. 컨트롤 (최종)
```
WASD      이동          마우스     조준
LMB(홀드)  사격          Space     대시
ESC       일시정지/메뉴   1·2·3/클릭 모듈 선택
R         재시작(사망후)  M         음소거    F11 전체화면(선택)
```

---

## 20. 기술 아키텍처 (no-CRT)
### 메인 루프 (고정 타임스텝)
- `QueryPerformanceCounter` dt 누산 → **sim 120Hz 고정**, 렌더 vsync. 일시정지/강화/히트스톱 시 sim 스킵.

### 디스플레이 / 커서
- 기본 해상도 **1280×720**(16:9). 전체화면 시 정수배 or 레터박스, 월드는 `glOrtho`로 해상도 독립.
- 렌더 60fps(vsync). sim 120Hz > 렌더 → 보간 불필요(최신 상태 렌더).
- **OS 커서 숨김**(`ShowCursor(FALSE)`) + 마우스 위치로 **커스텀 네온 십자선** 렌더.
- 창 리사이즈 시 `glViewport` 갱신, 종횡비 유지.

### 엔티티 (정적 풀, 힙 없음)
```
player ×1 / enemies[256] / bullets[256] / eBullets[256]  (구현 실측, v1.3)
particles[4096] / pickups[128] / modules(보유 스택 배열)
```
- 전역 정적 배열 + active 플래그 + 풀 재사용(스왑-삭제). malloc 불필요. 대형 버퍼는 `VirtualAlloc`.

### 수학 / RNG
- `vec2`. `sqrt`=`_mm_sqrt_ss`. `sin/cos`=다항 근사 or 256엔트리 테이블. `xorshift32` 전역(QPC 시드).

### 충돌 / 렌더 / 입력 / 오디오
- 원-타일·원-원 수동 분해(물리엔진 없음). `glOrtho` 월드, 즉시모드+가산 글로우, 도형/글리프 헬퍼.
- 입력: `WM_MOUSEMOVE`(→월드), `WM_LBUTTONDOWN/UP`, 키 상태 배열. 오디오: `waveOut` 2~3 버퍼.

### no-CRT 핵심
- `int _fltused=0;`. 엔트리 `WinMainCRTStartup`. 필요 시 `memset/memcpy` 자작(`#pragma function`). 종료 `ExitProcess`. 링크 `/NODEFAULTLIB`+명시 라이브러리.

---

## 21. 튜닝 상수 종합표
```c
// 플레이어
PLAYER_RADIUS 14  MAXSPEED 220  ACCEL 2000  FRICTION 2400
MAX_HP 6  IFRAME 1.0  DASH_SPEED 700  DASH_TIME 0.14  DASH_CD 0.8
// 무기
FIRE_INTERVAL 0.20  BULLET_SPEED 700  BULLET_R 5  BULLET_DMG 10  BULLET_LIFE 1.2
// 모듈 계수
DMG/STK 0.25  FIRERATE/STK 0.20  SPEED/STK 0.12(cap1.8) MULTI_SPREAD 12(cap+6)
CRIT/STK 0.15(cap1.0) CRIT_MULT 2.0  EXPLODE_R 60(+25/stk) DASHCD_MULT 0.75(floor0.2)
// 적 base@L1
HUNTER hp20 spd130 r13 / TURRET hp16 spd70 r14 shot1.6 spd280
RICOCHET hp12 spd230 r11 redir1.5 / FORK hp30 spd60 r18 ->shard hp8 spd160 r8 x2
// 스케일
HP 1+0.18*(d-1)  SPD min(1.6,1+0.04*(d-1))  CNT min(12,4+d)  ELITE min(0.25,0.03*(d-1))
// 엘리트 어픽스: shield재충전1.5 / swift+40% / volatile폭발r90뎀2 / clone4s / magnetic당김
// 보스 (k=d/3, 홀수CORE 짝수WARDEN)
BOSS_HP 400*(1+0.5*(k-1)) CORE_R50 WARDEN_R60 contactDmg2 RING16 ringCD2.5/1.5
// juice
SHAKE_MAX20 DECAY1.5  HITSTOP kill0.03 hit0.08 boss0.2
// 월드/점수
TILE48 SIM_HZ120 CAM_LERP0.12 LOOKAHEAD60
SCORE = depth*1000 + kills*25 + bits*5 + bossKills*500
```

---

## 22. 용량 예산 & 최적화
- 전부 절차생성 → **목표 < 64KB**(압축 없이). 1.44MB 사실상 비제약.
- 빌드 `cl /O1 /Os /GS- /Gy` + `link /NODEFAULTLIB /OPT:REF /OPT:ICF`. 최종(선택) Crinkler.
- `build.bat`가 매 빌드 exe 바이트 + 1.44MB 대비 % 출력.

---

## 23. 마일스톤 & 수용 기준
| MS | 기간 | 산출 | 수용 기준 |
|---|---|---|---|
| **M1** | 1~2주 | 엔진 골격 | 창/GL/고정루프/입력/PRNG/sqrt·sin, 플레이어 이동+벽충돌, exe<32KB |
| **M2** | 1주 | 던전 | 절차 생성·방·문·복도·다운링크, 카메라, 방간 이동 |
| **M3** | 1.5주 | 전투 | 발사체, 적 3종 AI, 데미지/사망/파티클, 전투방 문잠금-전멸-개방 |
| **M4** | 1주 | 루프 완성 | 레이어 하강, HUD, 점수, 게임오버/재시작, 기본 SFX → **MVP 완결** |
| **M5** | 2주 | 폴리시 | 모듈 14+3택1, 포크+엘리트 어픽스, 보스 2종(3레이어), 셰이크·히트스톱·유도/폭발 |
| **M6** | 1.5주 | 마감 | 사운드 전체, 팰릿 순환, 타이틀/일시정지/미니맵, 밸런싱, 용량 최적화, **제출** |
> 6/9 시작 ~M6까지 약 9주 + 버퍼 3주 (마감 9/4).

---

## 24. 리스크 & 대응
| 리스크 | 영향 | 대응 |
|---|---|---|
| 로그라이크 스코프 팽창 | 미완성 | MVP(M4) 우선 완결, 모듈/보스/엘리트는 폴리시 분리. 스트레치 폐기 가능 |
| 보스 2종 코딩 시간 | 일정 압박 | WARDEN을 M5 후반, 최악엔 CORE 1종으로 폴백 |
| no-CRT 함정 | 빌드 실패 | M1에서 최소 골격 선검증, 스택<4KB |
| 실시간 충돌·AI 튜닝 | 손맛 미달 | 상수표(§21) 빠른 반복, 결정론 시드 디버깅 |
| 밸런스 곡선 | 재미 저하 | §12 파라미터화, 시드 고정 플레이테스트 |

---

## 25. 리뷰 노트 & 동결 (Freeze)
**v1.0 리뷰 패스 — 점검·해소 항목:**
- ✅ 통화 명칭 BITS 전 섹션 일관. 깊이(내부)/LAYER(표시) 매핑 §2 명시.
- ✅ 보스 로테이션 k=깊이/3 (v1.0: 홀수 CORE·짝수 WARDEN 2종). **v1.3에서 3종 순환(CORE/WARDEN/NEXUS, (k-1)%3)으로 확장 — §26.**
- ✅ 적수 공식 min(12,4+깊이) ↔ §12 표 일치(L1=5 … L9=12 캡).
- ✅ 회복 경로 명문화(바닥 드롭 없음) + 자가 피해 없음 명시.
- ✅ 엘리트 모듈 오브 = 랜덤 커먼 1개 즉시(3택1 아님)로 확정.
- ✅ 보스방 잠금 규칙·다운링크 출현 시점 추가.
- ✅ 디스플레이(1280×720)·커서 숨김·커스텀 십자선·성능 추가.
- ✅ 옵션/접근성(셰이크·플래시 강도=광과민 대응·볼륨)·컨트롤러 미지원 명시.
- ✅ 사운드 신스 파라미터 전부 확정(§16.3).

**열린 항목(코드 단계에서 결정·튜닝):**
- 폭발/유도/반사 등 degenerate 수치는 M5 플레이테스트로 확정(§7 가드).
- 상점·BGM·미니맵 확대·정수배 스케일 = 스트레치(MVP 외).
- 회복 빡셈 → 12레이어 수리팩은 M5 재검토.

**동결 선언:** v1.0 기준 **MVP+폴리시 범위의 설계 고정**. 이후 변경은 코드 중 발견된 필요에 한해 changelog로 관리.

---

## 26. Changelog (post-freeze)

> v1.0 동결 이후의 변경. 용량 여유(최종 standalone exe가 하드캡 1.44MB의 5% 수준)를 활용한 승인된 확장.
> 모든 항목은 사용자 승인 하에 진행되며, 동결 섹션(§1~25)은 보존하고 본 changelog로 추적한다.

### v1.1 — 검증 패스 수정 (2026-06-13)
M4~M6+확장 구현 코드의 다차원 적대적 리뷰에서 적발·수정한 9건(CRIT/HIGH 0). 핵심:
- **결정론**: 카메라 추적 lerp를 dt-스케일로(프레임레이트 독립). burst/머즐 코스메틱 산란을 `g_rng`→`g_rngFx`로 분리(sim 시드 비오염). `g_rngFx` 0-시드 가드.
- **밸런스**: 재생(레어)을 GDD §7 명세대로 **비스택 25초/1HP**로 환원(스택 분할 제거). 엘리트 어픽스 2개 **중복 불가**(§8.5) 보장.
- **스펙 정합**: 자력 모듈 **소형 감속 오라**(§7) 구현. 게임오버 화면 `( BEST )` 줄(§18 목업). 포크 샤드 벽내부 스폰 폴백.

### v1.2 — 비주얼 폴리시 (2026-06-13, 사용자 ✅승인)
절차 렌더만으로 비주얼 밀도 강화(에셋 0개 유지, 셰이더리스 immediate-mode GL).
- **적/플레이어 다층 스프라이트**: 단일 도형 → 글로우+회전 플레이트+코어+상태큐(스폰/피격/저HP). 어픽스 5종 식별 글리프. 플레이어 짐벌 링+조준 핍. 색상 정체성·식별성 유지. 애니값은 `g_time`/`f_sin` 기반(결정론 안전).
- **보스 페이즈 시각화**: CORE 동심 페이즈 링+회전 아이리스, WARDEN 자이로 프레임+N각 무장링+코너 노드+세그먼트 테더. 페이즈 전환 juice(흰 플래시+이중 링+스파크).
- **셰이더리스 포스트FX** (`render_post`): 비네트+CRT 코너 / 스캔라인 / 색수차 프린지(히트·보스 페이즈) / 액션 헤이즈(의사 블룸). 광과민 대응: 펄스 FX는 `g_optFlash`, 정적 FX는 신규 마스터 `g_optCRT`(일시정지 `C` 토글 100/50/0).

### v1.3 — 콘텐츠 확장 (2026-06-13, ⏳비주얼 컨펌 대기)
**§7 모듈** — 14커먼+6레어 → **18커먼+8레어**:
- 신규 커먼 4: 보복(VENGEANCE, 저HP일수록 탄뎀↑), 운동(KINETIC, 이동속도일수록 탄뎀↑), 광란(FRENZY, 사격 홀드 시 연사속도 램프 캡+1.5), 흡수(SIPHON, N킬마다 BITS 다발+광란 너지 — 회복 아님).
- 신규 레어 2: 글래스캐논(GLASSCANNON, +60%뎀/스택 캡2 + 피격당 +1뎀 — 무한생존 차단 가드 내장), 체인라이트닝(CHAINLIGHT, 명중 시 인접 1체에 단발 아크, 재귀 없음).

**§8 적** — L5~L6 신규 2종(헌터/터릿/리코셰/포크 이후):
- **LANCER**(type5, L5 해금): 텔레그래프 돌진 스트라이커. 스토크(거리유지 슬라이드, 0.8s 예측선)→520px/s 돌진(0.35s, 벽 1회 반사)→회복. 돌진 속도는 고정(Swift는 사이클 타이밍만 단축).
- **WEAVER**(type6, L6 해금): 궤도 비행 기뢰 설치형. 중거리 공전 + 주기적 기뢰(`g_boom` 재활용, 1.4s 신관, r90/2뎀). 공간 압박.

**§9 보스** — 3종 순환으로 확장 (로테이션 `k=깊이/3`, `(k-1)%3`):
- **THE NEXUS**(type2, L9/L18/…): 노드망 존-컨트롤형(탄막 CORE·돌진 WARDEN과 차별). 3개 위성 노드를 잇는 회전 삼각 빔케이지(접촉 2뎀), 페이즈2 노드 방사 링, 페이즈3 노드 반경 수축 격노+본체 링, 터릿 주기 소환. 신규 구조체 필드 0개(기존 `segAng/shrink/t1~t3` 재활용).
- 순환: L3 CORE / L6 WARDEN / L9 NEXUS / L12 CORE …

**용량**: v1.0 동결 시점 대비 +12.8KB → 78,336 bytes (하드캡 1.44MB의 5.3%). 절차생성·no-CRT 원칙 유지.

### v1.4 — OVERCLOCK MODE O1 MVP (2026-06-18)
docs/07 SDD의 VS/세피리아式 호드 생존 모드를 **별도 모드**로 구현(O1 MVP). 본편 DESCENT 동결 설계 보존 — `g_mode` 단일 분기 + `oc_` 접두사 격리.
- **모드 분기**: 타이틀 `TAB`로 DESCENT/OVERCLOCK 전환. `new_run`이 `g_mode` 분기(아레나 vs 던전). `combat_update`는 OC 시스템 호출 후 DESCENT 방·다운링크 로직을 `g_mode==0` 게이트로 격리.
- **아레나**(`oc_arena_init`): 46×32 단일 방 + 기둥 엄폐물. **호드 스포너**(`oc_horde_update`): 시간 t로 밀도·종류 게이팅, `g_depth`를 t파생으로 두어 기존 HP/속도/엘리트 스케일 재활용. 활성 캡 `40+t*0.5`(MAXENE-8 상한).
- **자동사격**: PULSE는 발사 블록 자동조준(`acquire_target` §14.2 동점 타이브레이크). 신규 무기 7종 `oc_weapons_update`(전부 승인된 프리미티브 재활용): ORBITERS(공전 블레이드)·NOVA(최근접 폭발)·AURA(반경 장판 틱뎀)·SWARM(최근접 N체 조준 나이프)·BEAM(회전 스윕 레이저, `draw_beam`/`beam_hit`)·ARC(체인 라이트닝, 방문셋 추적)·DRONES(공전 자동사격 드론). 무기 캡 8 = SDD MAXWEAP 충족.
- **XP/레벨/드래프트**: 처치 시 XP 조각(픽업 kind=2) 드롭→자력 흡인→임계(`5+lv*4+lv²`)→레벨업 드래프트(`oc_draft`/`oc_apply`/`oc_capped` — draw3 불가침 §14.4). 무기 레벨업 + 커먼 패시브 모듈 3택1.
- **보스 웨이브**: 2분마다 본편 CORE/WARDEN/NEXUS 재활용(아레나 중앙 스폰). **`boss_spawn`/`boss_setup_bounds` 추출 + 경계 전역화(§14.3)** — 본편 보스 거동 수치 동일(회귀 0, 셀 산식 보존).
- **HUD/엔딩**: `oc_render_hud`(XP바·LV·타이머·킬). 사망 시 OC 점수(`floor(t)*10+kills*5+lv*200+boss*1000`)·최고 생존시간(`g_bestTime`) 갱신, 부패도 미증가(DESCENT 전용).
- **리팩터(중복 제거)**: 하트·피격 비네트·교신 오버레이·십자선을 공용 헬퍼(`draw_hearts`/`draw_hurt_vignette`/`draw_comms_overlay`/`draw_crosshair`)로 추출 → `render_hud`·`oc_render_hud` 공유.
- **BGM (§16.4)**: 절차 합성 BGM을 믹서(`snd_fill`)에 추가 — 120BPM, 팰릿 티어 루트, 아르페지오(sine 플럭)+베이스(saw)+킥(비트 1·3), 레이어 시드 변주. 일시정지 `B` 토글, `M` 음소거 포함. 게임플레이 상태에서만 재생. 양 모드 공용.
- **상점 (§13, DESCENT)**: ✅사용자 목업 승인(rules/50, `docs/design/shop-v1.png`) 후 구현. 비보스 레이어 35% 확률 전투방 1개를 상점방(type 4)으로 대체. 제단 3종 — 모듈(3택1) 60 / 수리(+1하트) 45 / 리롤(랜덤모듈) 30 BITS, 가격 +10%/L. 근접+`E` 구매, 구매 시 소진. 미니맵 `$` 앰버 마커. BITS=점수 자원이라 점수 vs 강함 트레이드오프 성립.
- **무기 진화 D-EVOLVE (§4.4)**: 무기 max(lv8) + 짝 패시브 보유 시 자동 진화(`g_weapEvo`, `g_evoReq` 페어링: PULSE+PIERCE/ORBIT+COOL/NOVA+POWER/AURA+THORNS/SWARM+MULTI/BEAM+RAPID/ARC+CRIT/DRONE+RAPID). 진화 시 해당 무기 파라미터 대폭 강화(개수·반경·뎀·연사·관통) + 교신 알림. `oc_check_evo`가 드래프트 적용마다 검사. 승인 프리미티브 재활용(비주얼 게이트 무관).
- **고정 시드 QA 훅 (§14.1)**: `cl /D ND_FIXED_SEED=N`(CI) + `set ND_SEED=N`(런타임) 2경로로 `g_master` 주입. 릴리스 빌드(미정의)는 QPC 그대로 → 동작 불변. 결정론 QA 재현(TC-OC-06) 가능.
- **OC 엘리트 웨이브 (SDD §3.5)**: 보스 웨이브(120s)와 함께 어픽스 엘리트 4~8체 일괄 스폰(`oc_spawn_at_edge` 반환 인덱스로 affix 강제).
- **BGM P3 격화 (§16.4)**: 보스 페이즈3 시 BGM 템포 +10% + 디튠(`g_bgmIntense` 플래그, boss_update 설정/boss_die 해제, bgm_sample 반영).
- **미니맵 Tab 확대 (§18 스트레치)**: 플레이 중 `Tab`으로 미니맵 2× 확대 토글(`g_mapZoom`, 셀/마커 배율 파라미터화).
- **F11 전체화면 (§18·§19)**: 보더리스 전체화면 토글(`toggle_fullscreen` — WS_POPUP↔WS_OVERLAPPEDWINDOW + 모니터 크기, GL 컨텍스트 재생성 없음, 매 프레임 glViewport가 적응).
- **OC 256적 배칭 렌더 (§14.6/§14.7)**: ✅사용자 목업 승인(`docs/design/oc-batch-v1.png`) 후 구현. MAXENE 128→256(.bss, 파일크기 무관). `draw_enemy_batched`(OVERCLOCK 전용): 패스당 단일 glBegin(글로우 1 + 삼각코어 1 + 사각/다이아코어 1 + 엘리트/스폰 소수 개별) → 248적도 ~수십 드로우콜(기존 1,700+ 대비). 적별 회전/글로우다겹/배지 단순화. 본편 DESCENT는 `draw_enemy` 다층 스프라이트 유지(웨이브 캡상 256 미사용 → 동작 불변). OC 호드 캡 자동 상향(40+t*0.5, MAXENE-8=248).
- **밸런스**: OC XP 픽업 자력 흡인 ×2.4(방 클리어 자동수거 없는 호드 모드 페이싱 — 플레이테스트로 발견).
- **용량**: 89,600 → **110,080 bytes**(+20.5KB, 하드캡 1.44MB의 7.5%). 검증: code-reviewer-game CRIT/HIGH 0(MED/LOW 5건 수정), DESCENT 회귀, 8무기·상점·BGM·진화·시드·엘리트웨이브·미니맵Tab·F11·256배칭호드(45s 밀집) — 전부 무크래시.
- **영상 녹화 인프라**: `#ifdef ND_REC` glReadPixels 프레임 덤프(릴리스 미포함) + `scripts/record-oc.ps1`·`assemble-rec.ps1`. ⚠️ 현 자동화 환경은 헤드리스라 GL 라이브 캡처 불가(PrintWindow/CopyFromScreen/glReadPixels 모두 픽셀 오너십으로 black/garbage). 실제 데스크톱에서만 녹화 가능. (자작 GIF-LZW 인코더는 비균일 프레임에서 미세 desync 잔존 — 실사용 시 ffmpeg 권장.)
- **미착수(외부 의존만 남음)**: ① OC 밸런스·인텐시티 튜닝 — 플레이테스트 피드백 필요(헤드리스라 자체 검증 불가). ② OC 보스/엔딩 + 256배칭 60fps 인게임 실측 — 실세션 필요(코드/안정성만 검증). ③ 영상 — 헤드리스 GL 캡처 불가(녹화 인프라 제공). **설계 문서상 코드 구현 항목은 전부 완료.**

### v1.3a — QA 밸런스 패스 (2026-06-13)
5차원 병렬 QA(밸런스·적·보스·버그·문서) 후 튜닝(CRIT 0; "5HP/틱" 류 발견은 `hurt_player` self-i-frame 가드로 무효 판정):
- **NEXUS 밀도 완화**: 노드 방사링 노드당 12→8발·인터벌 2.8→3.6s, 본체 링 16→12발, ph3 수축 18→12px/s, 빔케이지 halfw 전 페이즈 7 고정(보이지 않는 폭증 제거), 터릿 소환 **최대 2 + 8s + 방 경계 클램프**, 노드링 사운드 SFX_PHASE(위협음).
- **버그**: LANCER 돌진 코너 무한진동(±0.6r 2점 검사), WEAVER 벽 스터터(kvel 감쇠), GLASS CANNON 3스택 공허 추첨 방지(레어 풀 캡2), SIPHON 광란 너지 FRENZY 보유 시만(캡 존중), 픽업 풀 `MAXPICK` 128→256.
- **밸런스**: KINETIC +0.15→+0.22·게이트 완화(정지 +9%/전속 +22%), VENGEANCE 풀피 floor +5%(저체 +17%), LANCER 예측선 0.3→0.6s.
- 최종 **78,848 bytes** (캡 5.3%).

### v1.5 — 신규 적 PHANTOM (2026-06-22, 사용자 ✅승인)
- **PHANTOM (적 type 7, 위상 침입자)**: 주기적 순간이동 교란형. 느린 드리프트(0.5×spd) → 2.2s마다 0.5s 텔레그래프(목적지 고스트+점선) → 플레이어 방향 ~200px 블링크(오버슛/벽 진입 방지, dist-40 클램프). HP18/드리프트95/반경12/접촉1뎀. 스펙트럴 페일바이올렛, 이중 잔상 다이아+점멸 링.
- **해금**: DESCENT L7+, OVERCLOCK t≥180s. 엘리트 어픽스 적용. 양 모드 공용.
- **확장 구조 활용(0622 리팩터)**: `g_eneStat` 테이블 1행 + `enemy_update` 타입 분기 + `draw_enemy`/배칭 렌더 + `pick_enemy_type`/`oc_pick_type` 해금. rules/50 목업(`enemy-phantom-v1.png`) ✅승인 후 구현.
- 최종 **111,104 bytes** (캡 7.5%).

---
*v1.0 (FROZEN) — 전체 리뷰 통과·동결. 사운드/미니맵/상점/옵션/디스플레이 스펙 명세 완료.*
*v1.1~v1.3a (post-freeze) — §26 changelog 참조. 검증 수정 + 비주얼 폴리시 + 콘텐츠 확장 + QA 밸런스.*
*v1.4 OVERCLOCK 모드 · v1.5 PHANTOM 적 — §26 참조.*
